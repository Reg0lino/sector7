// assets/js/utils/helpers.js - Math, random generators, DOM 
export function getRandomInt(min, max) { return Math.floor(Math.random() * (max - min + 1)) + min; }

// Filename: helpers.js
// Directory: assets/js/utils/
